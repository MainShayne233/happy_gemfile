require 'spec_helper'

describe HappyGemfile do
  it 'has a version number' do
    expect(HappyGemfile::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
